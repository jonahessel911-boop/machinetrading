# Vanjeheftruckaf.nl

Landing funnel + CRM voor heftruck-bemiddeling.

## Starten

```bash
npm install
npx prisma migrate dev
npm run dev
```

- Site: http://localhost:3000
- CRM: http://localhost:3000/admin/login

Default login (zie `.env`): `admin` / `admin123`
